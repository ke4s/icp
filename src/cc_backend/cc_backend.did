type Profile = record {
    "hash": text;
    "name": text;
    "book_name": text;
    "subject": text;
    "description": text;
};

service : {
    "update": (text, text, text, text, text) -> (text);
};